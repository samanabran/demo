# Sync notes — 2026-08-31

## Source
- **Remote**: SSH alias `vps-root` → `/opt/odoo/demo_presentation/` (canonical container addon root).
- **Local target**: `C:/demo_presentation/` (this repo root).

## What was pulled
1. `addons/*` from `/opt/odoo/demo_presentation/addons/` → local root, preserving local state directories (`.git`, `.claude`, `.omc`, `.opencode`, `.superpowers`, `.github`, `docs`, `AGENTS.md`).
2. Root-level scripts: `docker-compose.yml`, `odoo.conf`, `demo_presentation_introspect.py`, `demo_presentation_migrate.py`, `demo_presentation_migrate_gallery.py`, `demo_presentation_seed_website.py`, `demo_presentation_verify.py`, `demo_presentation_verify_website.py`.
3. Audit outputs: `q11_*.csv` → `q19_*.csv` from `/opt/audit_readonly/out/demo_presentation/` → `scratchpad/audit_out_server/`.

## What was **not** pulled

- `addons_backup_*`, `db_backups`, `playwright-e2e`, `source_filestore` (operational, not addon code).
- `*.dump`, `*.backup.*` (database dumps, not addon code).
- `image.png`, `logo_*.png`, `icon_backup_*` (binary assets — handle through `git lfs` or the asset-pipeline, not in this sync).
- `osusproperties_v18.dump` (client DB dump — not addon code).

## What was added **after** the sync (this session)

- `30_QUARANTINE/` with five per-module READMEs (one per hold-list module).
- `sgc_realestate_brokerage_template/` — the reusable template addon (brokerage + ERP rollout for real estate).
- `docs/audit/MODULE_PROVENANCE.md`, `docs/audit/MULTI_TENANT_BLOCKERS.md`, `docs/audit/HARDCODED_COUPLING.md`, this file.
- `docs/REAL_ESTATE_BROKERAGE_ROLLOUT.md` — the rollout playbook.
- `tools/audit_coupling_lint.py` — the lint tool referenced from `MULTI_TENANT_BLOCKERS.md`.

## What was **not** changed

- `docker-compose.yml` — pulled from server unchanged. Audit-driven changes are documented in this repo's `30_QUARANTINE/README.md` and `docs/audit/HARDCODED_COUPLING.md` as actionable items; the change to `docker-compose.yml` is intentionally conservative at this commit (no modules removed) so the audit's hold-list evidence remains visible at the canonical path.

## Verification

```bash
# Verify the addon count matches the server:
ls /c/demo_presentation/ | wc -l   # → 56 (matches server addon count)
```

## Next sync marker

When this sync is repeated, overwrite this file with a fresh `SYNC_YYYY-MM-DD.md`, or rewrite this one. Don't accumulate stale sync notes.
