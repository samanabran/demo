# SGC TECH AI Theme — Launcher Build Progress

Append-only checkpoint log. Per repo CLAUDE.md "Interruption Resilience", this file is the resume checkpoint for any stage.

## Format
- `- [auto] <UTC ts> WRITE <path>` — mechanical, written by `.claude/hooks/log-progress.sh` on every Write/Edit under `addons/` or `artifacts/`.
- `- [STAGE<N>] <UTC ts> UNIT <what> -> <path> -- STATUS` — semantic, appended by the agent (or main agent acting as that stage) at each logical-unit boundary. STATUS is `DONE` or `IN-PROGRESS`.

## Entries

- [STAGE0] 2026-07-20T00:00:00Z UNIT plan-consensus -> artifacts/launcher-plan.md -- DONE (ralplan 3 rounds reached APPROVE; user-approved 2026-07-20 with: (1) palette = reuse shipped `appsbar.scss` base, no new accents; (2) execution = sequential `oh-my-claudecode:ralph`; (3) MiniMax `mmx-cli` Tier 1-supplement reference renders run in parallel with hand-authored SVG work)
- [STAGE1] 2026-07-20T01:00:00Z UNIT data-model:sgc.launcher.favorite,sgc.launcher.usage,res.users.launcher_* -> addons/sgc_tech_ai_theme/models/launcher.py + models/res_users.py + models/__init__.py + security/ir.model.access.csv -- DONE (US-001 schema foundations; 4 res.users fields, 2 new models with _sql_constraints, daily _cron_prune_usage SQL prepared, manifest data list updated; install verification pending Stage 2)
- [STAGE1] 2026-07-20T01:30:00Z UNIT session-info:launcher_*+sgc_theme_mode -> addons/sgc_tech_ai_theme/models/ir_http.py -- DONE (US-002; per-user prefs mirrored per-company entry; previously-dead sgc_theme_mode revived as deliberate bugfix per plan Follow-up #5; appsbar.js unchanged)
- [STAGE1] 2026-07-20T02:00:00Z UNIT launcher-trigger -> static/src/webclient/appsbar/appsbar.xml + appsbar.js -- DONE (US-003; AppsBar grid-icon button dispatches SGC:OPEN_LAUNCHER on env.bus; AppsBar keyboard menubar pattern and other behaviors untouched)
- [STAGE1] 2026-07-21T00:30:00Z UNIT launcher-service+root-component -> static/src/webclient/launcher/{launcher_service.js,launcher.js,launcher.xml,launcher.scss,launcher_webclient.js,launcher_webclient.xml} -- DONE (US-004; open/close lifecycle, focus trap, Esc handling, prefers-reduced-motion, forced-colors support; zero new hex colors)
- [STAGE1] 2026-07-21T00:35:00Z UNIT amend:US-003-aria-expanded -> static/src/webclient/appsbar/appsbar.js + appsbar.xml -- DONE (bus event replaced with sgc_launcher service so trigger's aria-expanded reflects real Launcher open state; discovered gap while implementing US-004, fixed same iteration)
- [STAGE1] 2026-07-21T01:15:00Z UNIT launcher-grid+card -> static/src/webclient/launcher/{launcher_grid.js,.xml,.scss,launcher_card.js,.xml,.scss} -- DONE (US-005; auto-fill responsive grid keyed on icon-size via [data-icon-size], hard 3-col mobile override, CSS-only ripple + prefers-reduced-motion; caveat logged in prd.json — large-icon desktop column count may land at ~5, one below the 6-8 target, needs live-browser confirmation at Stage 3)
- [STAGE1] 2026-07-21T01:45:00Z UNIT icon-family:36-svg -> static/src/webclient/launcher/icons/*.svg -- DONE (US-006; hand-designed glyph recipes stamped via a scratchpad-only generator, not shipped; palette verified to use only 4 already-shipped hex values across all 36 files; GAP: LauncherCard not yet wired to select from this set, still uses Odoo's app.webIconData — flagged for follow-up, not silently covered)
- [STAGE1] 2026-07-21T02:15:00Z UNIT icon-mapping -> static/src/webclient/launcher/launcher_icon_map.js + launcher_card.js -- DONE (US-006B, discovered gap promoted to a real story; 36 keyword rules resolved against app xmlid/name with webIconData fallback; verified all 36 rule keys have matching svg files; probabilistic-matching limitation disclosed since no Odoo core source is present to verify exact xmlids)
- [STAGE1] 2026-07-21T02:45:00Z UNIT launcher-search -> static/src/webclient/launcher/{launcher_search.js,.xml,.scss} + launcher.js + launcher.xml -- DONE (US-007; reads command_provider registry directly, never opens native Ctrl+K, fail-soft per provider; ASSUMPTION DISCLOSED — exact registry contract unverified against Odoo core source, needs live-instance confirmation at Stage 2 before Stage 3 tests can be trusted)
- [STAGE2] 2026-07-21T03:00:00Z UNIT dry-run:disposable-docker-stack -> scratchpad/odoo_stage2/{docker-compose.yml,odoo.conf} -- DONE (user-approved: spin up disposable stack rather than route via stale CLAUDE.md D:\ path or wait for user to point at real dev env; Odoo 19.0-20251222, port 8169, DB sgc_launcher_stage2, module mounted read-only at Odoo-canonical addons path per the sibling sgc_construction_management compose file's documented pattern)
- [STAGE2] 2026-07-21T03:10:00Z UNIT fix:sql-constraints-deprecation -> models/launcher.py -- DONE (live install surfaced 'Model attribute _sql_constraints is no longer supported' warning x2; migrated both models to models.Constraint(...) declarative API, exact syntax pulled from real Odoo 19 core source inside the running image (stock/models/stock_package_type.py); re-verified fresh install has zero warnings)
- [STAGE2] 2026-07-21T03:25:00Z UNIT fix:sass-min-max-incompatible-units -> static/src/webclient/launcher/launcher.scss -- DONE (live browser load surfaced 'A css error occured, using an old style to render this page' — root-caused via the actual failing-bundle file list in the browser console to launcher.scss's `min(1200px, 100%)`/`min(860px, 90vh)`, which Dart Sass evaluates as its OWN compile-time min/max requiring comparable units, not the CSS runtime function; fixed with max-width/max-height. Re-verified live: CSS compiles clean, Launcher renders with correct navy/gold styling)
- [STAGE2] 2026-07-21T03:40:00Z UNIT fix+verify:command-provider-namespace -> static/src/webclient/launcher/launcher_search.js -- DONE (US-007's disclosed namespace assumption was WRONG, corrected with hard evidence: live JS instrumentation via javascript_tool proved Odoo's real app/menu search lives under command_provider namespace '/', not the unnamespaced/'default' set assumed in the plan. Fixed filter, re-verified live: typing 'sett' returns real Settings menu matches, click navigates + closes cleanly, zero native Ctrl+K dialog ever opens)
- [STAGE2] 2026-07-21T03:50:00Z UNIT fix:stale-search-results-on-reopen -> static/src/webclient/launcher/launcher.js -- DONE (live testing found reopening the Launcher after a search showed stale results instead of the fresh grid; fixed by resetting searchState.results=null in close(); re-verified live)
- [STAGE2] 2026-07-21T04:00:00Z UNIT ruled-out:escape-key-false-alarm -- N/A (an apparent 'Escape does not close the dialog' failure was investigated via direct OWL/service instrumentation and traced to a testing-harness artifact — document.visibilityState='hidden' from switching between browser automation tabs stalls OWL 2's requestAnimationFrame-scheduled renders; re-tested with the tab properly foregrounded via `computer` actions and Escape closes correctly. NOT a code defect; documented so it isn't mistaken for one in a future session)
- [STAGE2] 2026-07-21T04:05:00Z UNIT teardown:disposable-stack -- DONE (docker compose down -v; stack was throwaway by design, scratchpad-only, no residue in the repo)
- [STAGE1] 2026-07-21T05:00:00Z UNIT favorites -> static/src/webclient/launcher/{launcher_grid.js,.xml,launcher.js,launcher.xml} + launcher_grid.scss -- DONE (US-008; orm-service CRUD on sgc.launcher.favorite, native HTML5 drag-reorder; pin-via-button not long-press, disclosed rationale: accessibility)
- [STAGE1] 2026-07-21T05:10:00Z UNIT recents -> static/src/webclient/launcher/launcher.js -- DONE (US-009; localStorage-only, per-user key, dedup, cap 8, zero server round-trip)
- [STAGE1] 2026-07-21T05:20:00Z UNIT frequently-used -> models/launcher.py (data/ir_cron.xml NEW) + static/src/webclient/launcher/launcher.js -- DONE (US-010; 2s trailing debounce, failure-silent orm.call; GAP FOUND+FIXED: _cron_prune_usage Python method from US-001 was never registered as an ir.cron record -- created data/ir_cron.xml, added to manifest)
- [STAGE1] 2026-07-21T05:35:00Z UNIT settings-panel -> static/src/webclient/launcher/{launcher_settings.js,.xml,.scss} + launcher.js/.xml -- DONE (US-011; 4 selects, gear trigger, seeded from session_info to avoid flash-of-default, orm.write persistence)
- [STAGE1] 2026-07-21T05:45:00Z UNIT background-styles -> static/src/webclient/launcher/launcher.scss + launcher.js -- DONE (US-012; solid/gradient/image/company_branding via [data-background-style], color-mix() gradient reusing shipped navy (zero new hex), --scrim-opacity:0.78 custom property per plan Follow-up #1; disclosed: image+company_branding share one source, no separate per-user upload field exists)
- [STAGE1] 2026-07-21T06:00:00Z UNIT accessibility-audit -> static/src/webclient/launcher/{launcher_grid.js,.xml,launcher_card.js,.xml,launcher_search.xml,.scss} -- DONE (US-013; REAL GAP FOUND+FIXED: grid had zero roving-tabindex/arrow-key nav despite existing since US-005 -- implemented mirroring AppsBar's exact pattern; disclosed gap: search stays role=searchbox not combobox, honest about missing aria-activedescendant rather than mislabeling)
- [STAGE3] 2026-07-21T06:15:00Z UNIT hoot-tests -> static/tests/launcher.test.js -- DONE (US-014; 5 tests A-E; UNVERIFIED AGAINST LIVE HOOT, disclosed in file header, needs /web/tests run)
- [STAGE3] 2026-07-21T06:30:00Z UNIT python-regression -> tests/test_regression.py -- DONE (US-015; test_07-test_12 added; REAL BUG CAUGHT WHILE WRITING: hardcoded git-diff path prefix would have false-failed in this repo's actual addons/sgc_tech_ai_theme/ structure, fixed to derive dynamically via git rev-parse --show-toplevel)
- [STAGE1] 2026-07-21T06:40:00Z UNIT branding-audit -> (no code changes, audit only) -- DONE (US-016; palette clean confirmed via grep across all launcher/* files, one grep false-positive ruled out (&#128269; emoji entity, not a hex color), pre-existing header gap on 2 untouched files disclosed but out of this story's scope)
- [STAGE1] 2026-07-21T06:45:00Z UNIT docs -> README.md + CHANGES.md (NEW) -- DONE (US-017; Theme Mode=Dark behavior change documented as required)
- [STAGE1] 2026-07-21T06:50:00Z UNIT gap-fix:cron-not-registered -> data/ir_cron.xml (NEW) + __manifest__.py -- DONE (found during US-010 re-audit: _cron_prune_usage Python method existed since US-001 but was never registered as an actual ir.cron record, so it would never have run in production)
- [STAGE1] 2026-07-21 ALL 18 STORIES (US-001 through US-017 + US-006B) MARKED passes:true in prd.json. Next: comprehensive live Docker re-verification pass covering everything built since the last live check (favorites CRUD, settings panel, background styles, recents, frequently-used, roving tabindex, hoot tests, Python regression tests, ir_cron.xml eval expression) before this can honestly be called Stage-1-complete.
- [STAGE3] 2026-07-21T07:30:00Z UNIT live-re-verify:python-tests -> tests/test_regression.py -- DONE (comprehensive Docker re-verification pass EXECUTED end-to-end 2026-07-21: spun disposable odoo:19.0 + postgres:16 stack back up (port 8169, DB sgc_launcher_stage2), reinstalled module clean (zero warnings), then `odoo-bin --test-enable --test-tags=/sgc_tech_ai_theme`. FIRST RUN: 0 failed, 2 errors of 12 — both errors in MY test code, not production code: test_09/test_10 called env['ir.http'].session_info() from a TransactionCase, but session_info() internally reads request.session.uid which only exists in a real HTTP request, throwing RuntimeError('object is not bound'). FIX: moved those two assertions to a new HttpCase class as test_13/test_14, fetching real /web and parsing the embedded odoo.__session_info__ JSON (same technique the pre-existing test_06 already used). test_12 correctly SKIPPED via the skip-on-no-git path — container image lacks git binary, skip-gracefully design worked as intended, not a failure. SECOND RUN: 0 failed, 0 errors of 12 tests — fully green. Live UI walkthrough also re-confirmed: Launcher dialog renders, settings panel populates and persists via orm.write, favorites pin works, recents and frequently-used populate correctly across reopen. REAL BUG CAUGHT AND FIXED DURING THIS PASS (already in test_regression.py at the time of the live re-run, was caught during the static-review pass that wrote test_12): hardcoded git-diff path prefix would have false-failed in this repo's addons/sgc_tech_ai_theme/ structure — fixed to derive dynamically via `git rev-parse --show-toplevel`. US-015 prd.json entry rewritten with actual live result; US-014 entry kept honest about still-unverified hoot /web/tests run. THEN stack torn down (`docker compose down -v`). Stage 1 + 2 + 3 honest status: SCAFFOLD COMPLETE, LIVE-VERIFIED, PYTHON TESTS GREEN. Stage 4 (readiness ≥95%), Stage 5 (production QA + feature-gap), Stages 6/7/8 (video/manual/marketplace) NOT yet started — awaiting user direction.)
- [STAGE1] 2026-07-21T07:35:00Z UNIT teardown-2:disposable-stack -> N/A (final `docker compose down -v` after the comprehensive re-verification pass completed; scratchpad-only stack, no residue in the repo)
- [STAGE1] 2026-07-21T08:00:00Z UNIT gap-fix:headers -> addons/sgc_tech_ai_theme/models/{res_company.py,res_config_settings.py} -- DONE (Gap A: added the standard (c) SGC TECH AI header to two pre-existing untouched files disclosed out-of-scope in US-016 audit; verified on disk)
- [STAGE1] 2026-07-21T08:30:00Z UNIT gap-fix:per-user-background-upload -> addons/sgc_tech_ai_theme/{models/res_users.py,models/ir_http.py,static/src/webclient/launcher/launcher.js,launcher_settings.js,launcher_settings.xml,launcher_settings.scss,launcher.xml} -- DONE (Gap B: new res.users.launcher_background_image Binary field, has_launcher_background_image bool mirrored in session_info, backgroundImageUrl getter branches on style (image->res.users, company_branding->res.company), settings panel UI gets conditional file input + remove button when background_style=image, FileReader-based upload via orm.write + imageVersion cache-buster; the 'image' and 'company_branding' settings now actually resolve to different sources instead of the prior disclosed shared fallback)
- [STAGE1] 2026-07-21T09:30:00Z UNIT gap-fix:combobox-search -> addons/sgc_tech_ai_theme/static/src/webclient/launcher/{launcher_search.js,launcher_search.xml,launcher_search.scss,launcher.xml} + launcher.js (capture-phase listener removed so search input's stopPropagation actually works) -- DONE (Gap C: full WAI-ARIA 1.2 combobox+listbox+option pattern; role=combobox on input with aria-autocomplete=list + aria-activedescendant; result listbox with stable option ids and aria-selected; ArrowDown/Up/Home/End navigation cycling through results; Enter activates the active descendant; polite live region announces result counts; the prior disclosed 'role=searchbox not combobox' gap is closed)
- [STAGE3] 2026-07-21T10:30:00Z UNIT gap-fix:hoot-test-wrapper -> addons/sgc_tech_ai_theme/tests/{__init__.py,test_hoot_launcher.py} -- PARTIAL (Gap D: created test_hoot_launcher.py subclassing HOOTCommon from web.tests.test_js; mirrors WebSuite's pattern; invokes browser_js against /web/tests?filter=/SGC Launcher/ with success_signal=[HOOT] Test suite succeeded. tests/__init__.py updated to import the wrapper. Two real bugs caught during construction: (1) @odoo.tests.no_retry decorator needed explicit `import odoo.tests` (NameError silently excluded the file from loader); (2) tests/__init__.py needed an explicit `from . import test_hoot_launcher` line or the wrapper wasn't even importable. BOTH fixed; verified clean import inside the running container; loader's get_module_test_cases returns the class. END-TO-END EXECUTION via `odoo-bin -i sgc_tech_ai_theme --test-enable` did run the wrapper after fixing the DB install path (-i not -u, container must run as root for apt-get, chromium-browser package installed, websocket-client pip-installed, no-sandbox wrapper script + symlinks created): TestSgcLauncherHoot.test_launcher_hoot shows 'Starting' in logs and reaches browser_js but Chrome cannot start a working DevTools port within 10s. SAME blocker affects all browser_js tests in the suite (WebSuite.test_hoot, test_click_everywhere.*, web_tour.*, etc. all skipped for the same reason). User authorized the --no-sandbox wrapper as the standard ephemeral-container pattern; wrapper is installed but Chrome still won't bind DevTools — likely odoo's chrome discovery in 19.0 either ignores /usr/bin symlinks or needs an additional argument the wrapper doesn't supply. HONEST STATUS: wrapper is correctly constructed, the test class IS invoked end-to-end through browser_js, and only Chrome's startup blocks final verification. prd.json US-014 entry rewritten to disclose this rather than silently look like a pass.)
- [STAGE3] 2026-07-21T11:00:00Z UNIT root-cause+fix:chrome-devtools-startup -> (env-only fix, no addon code changed) -- DONE (Gap D CLOSED with hard evidence, no speculation. ROOT CAUSE, proven by direct reproduction in a fresh disposable odoo:19.0 container: on this image's Ubuntu 24.04 (Noble) base, `apt-get install chromium-browser` resolves to Ubuntu's snap-transitional stub package (apt candidate version literally `2:1snap1-0ubuntu2`) — `/usr/bin/chromium-browser` is a 60-line shell script whose first check is `if ! [ -x /snap/bin/chromium ]; then echo "requires the chromium snap..."; exit 1; fi`. Since snapd never runs inside a Docker container, this exits 1 immediately, EVERY time, before spawning any real browser process — so `DevToolsActivePort` can never be written and Odoo's `_spawn_chrome()` (odoo/tests/common.py) always hits its 10.0s timeout (`CHECK_BROWSER_SLEEP=0.1 * CHECK_BROWSER_ITERATIONS=100`, hardcoded, not configurable via any browser_js/test_tags argument) and raises `unittest.SkipTest`. Confirmed live: `timeout 8 /usr/bin/chromium-browser --headless --no-sandbox ...` printed exactly `Command '/usr/bin/chromium-browser' requires the chromium snap to be installed.` and exited 1. This also explains the prior session's wrapper/symlink attempts and the "recursive wrapper" bug — they were fighting the snap-stub, not a sandbox/namespace/cgroup restriction; Docker capabilities, /dev/shm (confirmed 64M, and Odoo already unconditionally passes --disable-dev-shm-usage in its own headless_switches, so the custom wrapper's flags were redundant), and kernel sandboxing were never the blocker. FIX: install Google Chrome's official non-snap `.deb` (`google-chrome-stable`) instead of `chromium-browser` — `odoo/tests/common.py:_find_executable()` already searches PATH for `google-chrome` first, before `chromium`/`chromium-browser`, so no wrapper or symlink is needed at all. Verified live in isolation first (launched Chrome with Odoo's exact flag set, `DevToolsActivePort` file appeared immediately with a real port + `DevTools listening on ws://127.0.0.1:...`), then end-to-end: fresh disposable stack (docker-compose in OS scratchpad, odoo:19.0 + postgres:16, addons bind-mounted read-only), `google-chrome-stable` + `websocket-client` installed (no wrapper, no symlinks), `odoo -i sgc_tech_ai_theme --test-enable --test-tags=/sgc_tech_ai_theme --stop-after-init` run against a fresh DB: TestSgcLauncherHoot.test_launcher_hoot's Chrome pid launches, DevTools websocket connects, HOOT runner loads and executes all 5 US-014 sub-tests. Chrome-startup outcome: FULLY RESOLVED, matching the "Outcome A" bar exactly (Chrome starts, DevTools endpoint available, HOOT test executes).)
- [STAGE3] 2026-07-21T11:00:00Z UNIT gap-fix:hoot-suite-bugs -> addons/sgc_tech_ai_theme/static/tests/launcher.test.js -- DONE (with Chrome no longer blocking, the live run surfaced 3 REAL, PRE-EXISTING bugs in launcher.test.js itself that had never been executable before today — none are Chrome/DevTools issues. (1) `sgc.launcher.favorite`/`sgc.launcher.usage` RPCs failed with "could not get model from server environment" — hoot's mock server needs explicit `defineModels()`; added minimal mock model classes for both. (2) That fix then surfaced `res.users` itself was undefined in the mock server ("did you forget to use defineModels()?") — `defineWebModels()` (which registers the framework's default res.users/res.partner/etc. mock set) is NOT implicit once any custom `defineModels()` call is made; added the missing `defineWebModels()` call. (3) US-014-A's `queryOne('#sgc_launcher_root', { exact: false })` threw `HootDomError: found 0 elements instead of 1` — confirmed against the real hoot-dom source (`QueryOptions` typedef in odoo/addons/web/static/lib/hoot-dom/helpers/dom.js) that `exact` is not a real option; `queryOne` always forces `count:1` internally and cannot assert absence. Fixed to `expect(queryAll('#sgc_launcher_root').length).toBe(0)`. (4) US-014-D failed with "expected at least 1 assertion ... but none were run" — its `if (cards.length > 1)` guard never fired because a bare `makeMockEnv()` seeds zero apps for `menuService.getApps()`; added `defineMenus([...3 top-level apps...])`. All 4 fixes verified one-at-a-time against fresh DBs (sgc_hoot_repro2 through sgc_hoot_repro4) per systematic-debugging discipline — each re-run's log confirms progressively more tests passing until the final run: `[HOOT] Test suite succeeded`, `Passed 5 tests (8 assertions...)`, `0 failed, 0 error(s) of 13 tests when loading database`. Disposable stack torn down after (`docker compose down -v`), scratchpad-only, no residue in the repo. US-014 can now honestly be marked fully live-verified.)

