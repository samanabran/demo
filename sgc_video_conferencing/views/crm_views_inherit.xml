<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="crm_lead_form_view_inherit_video" model="ir.ui.view">
        <field name="name">crm.lead.form.video.inherit</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_schedule_meeting" type="object"
                        class="oe_stat_button" icon="fa-video-camera">
                    <div class="o_stat_info">
                        <field name="video_meeting_count"/>
                        <span class="o_stat_text">Meetings</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>

    <record id="crm_lead_view_kanban_inherit_video" model="ir.ui.view">
        <field name="name">crm.lead.kanban.video.inherit</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.view_crm_lead_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//footer" position="inside">
                <div class="d-flex justify-content-end">
                    <div class="me-2" t-if="record.video_meeting_count.raw_value">
                        <i class="fa fa-video-camera"/> <t t-esc="record.video_meeting_count.value"/>
                    </div>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
