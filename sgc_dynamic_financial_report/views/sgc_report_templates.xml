<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (c) 2026 SGC TECH AI (https://sgctech.ai) -->
<!-- License OPL-1 -->
<odoo>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- PDF QWEB TEMPLATE – base template for PDF export                 -->
    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Odoo 19 passes the records to render as ``docs`` (not ``object``
         as older releases did) via ``ir.actions.report``'s render path.
         ``docs`` is the recordset of wizard instances - we expect the
         wizard to call ``report_action`` for a single record, so use
         ``docs[0]`` for the body.

         No separate title block here: ``docs[0].result_html`` (built by
         ``_build_report_header_html`` in the engine) already renders its
         own company name / report title / date range / currency line -
         adding another one here duplicated that block verbatim in the
         PDF. ``web.external_layout`` still supplies Odoo's own
         logo/address header and page-number footer around it. -->
    <template id="sgc_financial_report_template">
        <t t-call="web.html_container">
            <t t-call="web.basic_layout">
                <t t-set="title">SGC Financial Report</t>
                <div class="page sgc_pdf_document">
                    <div class="sgc_report_body">
                        <t t-raw="docs[0].result_html"/>
                    </div>
                    <div class="sgc_report_footer" t-if="docs[0].company_id.sgc_dfr_report_footer">
                        <p t-esc="docs[0].company_id.sgc_dfr_report_footer"/>
                    </div>
                </div>
            </t>
        </t>
    </template>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Inline QWeb template for HTML report rendering                  -->
    <!-- (Used by the report engine to build the result_html field)      -->
    <!-- ════════════════════════════════════════════════════════════════ -->
    <template id="sgc_report_table_template" name="SGC Report Table">
        <div class="sgc-report">
            <table class="table table-bordered table-sm o_sgc_report_table">
                <thead>
                    <tr class="table-primary">
                        <t t-foreach="columns" t-as="col">
                            <th t-esc="col"/>
                        </t>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="lines" t-as="line">
                        <tr t-attf-class="{{line.get('css_class', '')}}">
                            <t t-foreach="columns" t-as="col">
                                <td t-esc="line.get(col, '')"/>
                            </t>
                        </tr>
                    </t>
                </tbody>
                <tfoot t-if="totals">
                    <tr class="font-weight-bold bg-light">
                        <t t-foreach="columns" t-as="col">
                            <td t-esc="totals.get(col, '')"/>
                        </t>
                    </tr>
                </tfoot>
            </table>
        </div>
    </template>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Configuration menu under Settings                                -->
    <!-- ════════════════════════════════════════════════════════════════ -->
    <record id="sgc_dfr_menu_configuration" model="ir.ui.menu">
        <field name="name">Configuration</field>
        <field name="parent_id" ref="sgc_dfr_menu_root"/>
        <field name="sequence">999</field>
        <field name="group_ids" eval="[(6, 0, [ref('sgc_dfr_group_manager')])]"/>
    </record>

    <menuitem
        id="sgc_dfr_menu_account_type_mapping"
        name="Account Type Mappings"
        parent="sgc_dfr_menu_configuration"
        action="sgc_dfr_action_account_type_mapping"
        sequence="10"
        groups="sgc_dynamic_financial_report.sgc_dfr_group_manager"
    />

</odoo>